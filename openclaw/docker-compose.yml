
services:
  openclaw:
    image: openclaw/openclaw:latest
    restart: unless-stopped
    environment:
      - OPENCLAW_ENV=production
    ports:
      - "127.0.0.1:8787:8787"
    networks:
      - openclaw_net

networks:
  openclaw_net:

